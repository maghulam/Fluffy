pipeline {
  agent any
  stages {
    stage('Fluffy Build') {
      steps {
        echo 'Placeholder'
        bat 'echo Another Placeholder.'
      }
    }

    stage('Fluffy Test') {
      steps {
        bat 'timeout /t 5 /nobreak'
        bat 'echo Success!'
      }
    }

    stage('Fluffy Deploy') {
      steps {
        echo 'Placeholder'
      }
    }

  }
}